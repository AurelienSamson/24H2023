{% extends 'base.html.twig' %}

{% block title %}Circuits optionnels{% endblock %}

{% block body %}

<div class="row">


{% for item in images_circuit_option %}

 <div class="col-sm-3">
  <div class="card">
   <img src="/assets/images/races/optionnelles/{{ item.image }}" class="card-img-top" alt="...">
   <div class="card-body">
     <p class="card-text">{{ item.name }}</p>
   </div>
  </div>
  </div>


    
{% endfor %}

</div>

{% endblock %}
