{% extends 'base.html.twig' %}


{% block body %}
<h2 class="text-uppercase">{{title}}</h2>

{# {% for items in inventaires %}
    {{ dump(items[]) }}
{% endfor %} #}

{# {{ dump(inventaires) }} #}

{% endblock %}